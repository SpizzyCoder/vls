# vls
vls is a command line tool similar to ls.
It aims to be as simple and lightweight as possible.

# Building
```
git clone https://gitlab.com/SpizzyCoder/vls.git
cd vls
cargo install --path .
```
The executable is now located in your .cargo/bin folder (usually ready to use).
